VITE_APP_NAME=SaaS Core Template
# TODO(deployment): For production builds, replace this with the public API origin
# for the SaaS environment, for example https://api.example.com.
VITE_API_BASE_URL=http://localhost:8000
# TODO(deployment): Replace with the public frontend URL for each environment.
VITE_PUBLIC_SITE_URL=http://localhost:5173
