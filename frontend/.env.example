VITE_APP_NAME=SaaS Core Template
# TODO(deployment): For production builds, replace this with the public API URL
# for the SaaS environment, for example https://api.example.com/api/v1.
VITE_API_BASE_URL=http://localhost:8000/api/v1
# TODO(deployment): Replace with the public frontend URL for each environment.
VITE_PUBLIC_SITE_URL=http://localhost:5173
